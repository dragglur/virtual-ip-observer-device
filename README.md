# virtual-ip-observer-device

Виртуальный "прибор" в виде серверного приложения, позволяющий узнать публичный IP-адрес сервера и IP-адреса подключенных к нему сетевых адаптеров(vpn, wifi/ethernet, etc.)
